#[coordinates]
#base = 'z'
#fiber = 'x'

[symbolic parameters]
# SL2C_params = []

[analytic expressions]
sw_curve = x**3 + v2*x + v3 + z**2
### \lambda = v(x, z) dz
sw_diff_v = x

#sw_curve = (1 + x + x**3)*(z - 1)**2 + (z + 1)**2
#sw_diff_v = -2*x/(z - 1)**2

[Seiberg-Witten parameters]
v2 = 1.0
#v2 = 0.0
v3 = 1.0

[numerical parameters]
#phase = 1.0
phase_range = [0.0, pi, 10]

#default range limits as [z.real.min, z.real.max, z.imag.min, z.imag.max]
z_range_limits = [-5, 5, -5, 5]

num_of_steps = 10
num_of_iterations = 2
size_of_small_step = 1e-2
size_of_large_step = 1e-1

size_of_neighbothood_in_step = 2 * %(size_of_large_step)s
size_of_puncture_cutoff = 2 * %(size_of_small_step)s
size_of_ramification_pt_cutoff = 1e-3

size_of_bin = %(size_of_small_step)s

accuracy = 1e-6
